import Api from "../../../services";

export const load = async ({ params }) => {
  // const response = await Api.get("/api/rest/product/product_detail?id=" + params.slug);
  return { product: {} };
}
